# Overview
Low-dose CT reduces radiation at the cost of noise/artifacts. LightGAN-LD targets efficient high-fidelity reconstruction via a learnable Sinogram Encoder + Ghost/CondConv/ECA generator + PatchGAN discriminator with composite losses and robust training strategy.
